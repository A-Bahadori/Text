chore(build): remove obsolete packages.config after SDK-style migration

- The packages.config file was accidentally retained during the .csproj SDK-style conversion.
- This commit cleans up the project by removing the redundant NuGet configuration file.
