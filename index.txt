feat(web): configure default document in web.config for serving index.html

- Updated the `web.config` file to set `index.html` as the default document.
- Added `<defaultDocument>` configuration under `<system.webServer>` to serve `index.html` as the landing page.
- Removed previous changes from `.csproj` related to IIS URL configuration.
