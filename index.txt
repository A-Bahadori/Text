try
            {
                Console.WriteLine("برنامه تست پردازش فایل Word");
                
                // مسیر فایل
                Console.Write("مسیر فایل Word را وارد کنید: ");
                string filePath = Console.ReadLine().Trim();
                
                if (!File.Exists(filePath))
                {
                    Console.WriteLine("فایل مورد نظر یافت نشد.");
                    return;
                }
                
                // ایجاد IFormObject
                IFormObject formObject = CreateFormObjectFromFile(filePath);
                
                // ایجاد نمونه از کلاس پردازشگر
                var processor = new YourProcessor(); // نام کلاسی که GetWordFormat در آن قرار دارد
                
                // تنظیم نوع خروجی (Word یا PDF)
                Console.Write("آیا خروجی به صورت PDF باشد؟ (y/n): ");
                bool isPdf = Console.ReadLine().Trim().ToLower() == "y";
                
                // فراخوانی متد پردازش
                byte[] result = processor.GetWordFormat(formObject, Path.GetFileName(filePath), isPdf);
                
                // تعیین پسوند فایل خروجی
                string extension = isPdf ? ".pdf" : ".docx";
                
                // تعیین مسیر خروجی
                string outputPath = Path.Combine(
                    Path.GetDirectoryName(filePath),
                    Path.GetFileNameWithoutExtension(filePath) + "_output" + extension
                );
                
                // ذخیره نتیجه
                File.WriteAllBytes(outputPath, result);
                
                Console.WriteLine($"فایل با موفقیت پردازش شد و در مسیر زیر ذخیره شد:");
                Console.WriteLine(outputPath);
            }
            catch (Exception ex)
            {
                Console.WriteLine($"خطا
