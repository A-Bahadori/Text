FindReplaceOptions options = new FindReplaceOptions();
options.ReplacingCallback = new ReplaceEvaluatorFindAndInsertText(entVal.ToString().Replace("\r", "\n"));
options.UseRegularExpressions = true;
doc.Range.Replace(Regex.Escape(p.Value), "", options);

doc.Range.Replace(Regex.Escape(p.Value), new ReplaceEvaluatorFindAndInsertText(entVal.ToString().Replace("\r", "\n")), false);

doc.Range.Replace(p.Value, new ReplaceEvaluatorFindAndInsertText(entVal.ToString().Replace("\r", "\n")), false);
