revert(web): rollback default document configuration in web.config

- Reverted changes made to the `web.config` file related to setting `index.html` as the default document.
- Removed `<defaultDocument>` configuration under `<system.webServer>`.
